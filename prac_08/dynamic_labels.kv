BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'vertical'
        # this layout is populated with Label widgets in app code
        id: entries_box
    Label:
        size_hint_y: 0.1
        text: app.status_text
